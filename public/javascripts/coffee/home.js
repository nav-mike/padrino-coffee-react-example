// Generated by CoffeeScript 1.9.2

/** @jsx React.DOM */

(function() {
  $(document).ready(function() {
    var PasswordFields;
    PasswordFields = React.createClass({
      ERROR_CLASS: 'has-error',
      SUCCESS_CLASS: 'has-success',
      getInitialState: function() {
        return {
          classVal: 'form-group'
        };
      },
      handleChange: function() {
        var confirm, password;
        confirm = React.findDOMNode(this.refs.confirm).value.trim();
        password = React.findDOMNode(this.refs.password).value.trim();
        if (confirm !== password) {
          this.setState({
            classVal: "form-group " + this.ERROR_CLASS
          });
        }
        if (confirm === password) {
          return this.setState({
            classVal: "form-group " + this.SUCCESS_CLASS
          });
        }
      },
      render: function() {
        return <div className="passwordFields">
          <form className='form-inline'>
            <div className={this.state.classVal}>
              <input type='password' className='form-control' ref='password' placeholder='Password' />
            </div>
            <div className={this.state.classVal}>
              <input type='password' className='form-control' ref='confirm' placeholder='Confirm password' onChange={this.handleChange} />
            </div>
          </form>
        </div>;
      }
    });
    return React.render(<PasswordFields />, document.getElementById('reactApp'));
  });

}).call(this);
